# Questiona Create a function that takes a word from user and translates it using a dictionary of three words, 
#returning a message when the word is not in the dict,
# considering user may enter different letter cases
d = dict(weather = "clima", earth = "terra", rain = "chuva")






#Answers

#def vocabulary(word):
#    try:
#        return d[word]
#    except KeyError:
#        return "That word does not exist."

#word = input("Enter word: ").lower()
#print(vocabulary(word))
